# Example environment variables for this project

# OpenAI API key (server-side)
OPENAI_API_KEY=

# n8n webhook URL
N8N_WEBHOOK_URL=

# Optional: n8n webhook auth token
N8N_WEBHOOK_AUTH=

# Supabase configuration
# Public project URL (example: https://<project>.supabase.co)
SUPABASE_URL=https://ahfkmsnompkgdvnuaeup.supabase.co

# Supabase Service Role Key (server-only, DO NOT commit real service keys to public repos)
# Replace with your service_role key in your local .env
SUPABASE_SERVICE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY

# Optional: Supabase anon/public key (client-side). Not required on server.
SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY

# Storage bucket name used for stickers
SUPABASE_BUCKET=stickers

# Optional: folder/prefix inside the bucket where stickers will be stored
SUPABASE_UPLOAD_FOLDER=stickers

# Public URL base for objects in the storage bucket (convenience)
# Example: https://<project>.supabase.co/storage/v1/object/public
SUPABASE_PUBLIC_URL=https://ahfkmsnompkgdvnuaeup.supabase.co/storage/v1/object/public

# Maximum accepted upload size (in bytes). Adjust if needed.
SUPABASE_MAX_UPLOAD_SIZE=10485760
